# Write a Python program to convert a list of characters into a string.

def convert(s): 

    # initialization of string to "" 
    str1 = "" 
    
    return(str1.join(s)) 

# driver code
s = ['p', 'a', 'r', 't', 'h'] 
print(convert(s))